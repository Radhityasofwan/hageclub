// Status order yang sudah masuk alur pengiriman — hanya ini yang bisa dilacak.
// File tanpa "use client" supaya bisa dipakai server component dan client.
export const TRACKABLE_STATUSES: string[] = ["PROCESSING", "PACKED", "SHIPPED"];
